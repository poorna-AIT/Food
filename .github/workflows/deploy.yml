name: Deploy Food Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Trigger Deployment Script
        run: |
          cd /home/ubuntu/deploy
          ./deploy.sh